# Implementation Plans

Generated by the `improve` skill (deep audit) on 2026-06-24, against commit
`4b41613`; extended on 2026-06-24 with a post-fix re-scan against commit
`a188e11` (Plans 016-019); extended again on 2026-07-01 (commit `56226fb`)
with a `plan` invocation scoped to the cart/checkout flow (Plan 020) plus a
Square-implementation deep dive, all four findings from which the maintainer
selected for planning (Plans 021-024); extended again on 2026-07-22 (commit
`89d7c1d`) with a full-repo audit — P1 quick wins (Plans 027-029) and the
performance cluster (Plans 030-033) — all of which executed and landed in
the same session. Extended a final time on 2026-07-22 (commit `0da82aa`)
with security + correctness + coverage plans (Plans 034-041). Extended again
on 2026-07-22 (commit `9b5556b`) with a fresh deep audit covering security,
architecture, dependencies, and DX — Plans 042-058. Extended on 2026-08-01
(commit `915a062`) with a full deep audit — bugs, security, performance, test
coverage, architecture, dependencies, and DX — Plans 074-105 (Plans 074-075
written in the previous session; 076-105 in this session). Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Category | Status |
|------|-------|----------|--------|------|------------|----------|--------|
| 020  | Merge cart items into the sticky order-summary sidebar | P1 | M | MED | — | direction | DONE |
| 021  | Add retry protection to the two unprotected checkout-critical Square calls | P1 | S | LOW | — | bug | DONE |
| 022  | Make the cart-total preview use authoritative Square pricing | P1 | S | LOW | — | bug | DONE |
| 023  | Single-source the free-shipping threshold and flat rate | P2 | S | LOW | — | tech-debt | DONE |
| 024  | Consolidate the Square resilience layer onto `apiRetryClient` | P2 | M | MED | 025, 026 | tech-debt | DONE |
| 025  | Mock the Square inventory call in filterCaching.test.ts | P1 | S | LOW | — | tests | DONE |
| 026  | Mock the Square inventory call in filterPerformance.test.ts | P1 | S | LOW | — | tests | DONE |
| 017  | Fix the failing src/lib/cart/index.ts coverage threshold | P1 | S | LOW | — | tests | DONE |
| 018  | Patch high-severity dependency advisories, migrate overrides to pnpm-workspace.yaml | P1 | S | LOW | — | dependencies | DONE |
| 001  | Route check-inventory.ts through the consolidated inventory core | P1 | S | LOW | — | tech-debt | DONE |
| 004  | Close the data: URI gap in WordPress HTML sanitization | P1 | S | LOW | — | security | DONE |
| 007  | API route test pattern (admin-auth + create-checkout) | P1 | L | MED | — | tests | DONE |
| 002  | Replace ad-hoc SquareClient instantiations with the shared singleton | P2 | M | MED | — | tech-debt | DONE |
| 019  | Remove dead code (errorRecovery, loadingManager/loadingStates, logoSvg) | P2 | S | LOW | — | tech-debt | DONE |
| 003  | Consolidate saleVisibility.ts and shopVisibility.ts | P2 | M | LOW | — | tech-debt | DONE |
| 005  | Stop leaking internal error details in API responses | P2 | S | LOW | — | security | DONE |
| 010  | Reconcile stale docs (PRELAUNCH_TODO, BACK_IN_STOCK) | P2 | S | LOW | — | docs | DONE |
| 011  | Add CLAUDE.md | P2 | S | LOW | — | dx | DONE |
| 013  | Populate sitemap customPages (products/categories/news) | P2 | M | MED | — | direction | SUPERSEDED by 016 (importing `src/lib/...` into `astro.config.mjs` fails: the `@` alias is defined inside the same `defineConfig()` call it would need to be resolved by, before Vite exists — chicken-and-egg, confirmed by reading `astro.config.mjs:80-89`) |
| 016  | Populate sitemap customPages, alias-free (supersedes 013) | P2 | M | MED | — | direction | DONE |
| 027  | Patch @astrojs/netlify to 8.1.2 (GHSA-hp3v security advisory) | P1 | S | LOW | — | security | DONE |
| 028  | Fix CLAUDE.md — correct Astro version from 6 to 7 | P1 | S | LOW | — | docs | DONE |
| 029  | Fix stale E2E button selector in cart-flow.spec.ts | P1 | S | LOW | — | tests | DONE |
| 030  | Remove debug artifacts from src/lib/square/categories.ts | P2 | S | LOW | — | tech-debt | DONE |
| 031  | Add unit tests for src/lib/square/money.ts | P2 | S | LOW | — | tests | DONE |
| 032  | Add pre-commit hooks (typecheck + tests) | P2 | S | LOW | — | dx | DONE |
| 033  | Remove unplugged RUM performance monitoring infrastructure | P2 | M | LOW | — | perf | DONE |
| 034  | Fix reflected XSS in AppliedFilters pill builder | P1 | S | HIGH | — | security | DONE |
| 035  | Escape user-controlled HTML in transactional email templates | P1 | S | MED | — | security | DONE |
| 036  | Upgrade sharp and clear pnpm audit critical/high advisories | P1 | S | HIGH | — | dependencies | DONE |
| 037  | Add item caps and rate limits to public batch endpoints | P1 | S | LOW | — | security | DONE |
| 038  | Persist failed webhook email deliveries for admin retry | P2 | M | MED | — | bug | DONE |
| 039  | Add unit tests for email layer (templates.ts + sender.ts) | P2 | M | LOW | 035 (soft) | tests | DONE |
| 040  | Add unit tests for Square webhook handler | P2 | M | LOW | 038 (soft) | tests | DONE |
| 041  | Quick wins: unused deps, dead code, .env.example gaps | P2 | S | LOW | — | tech-debt | DONE |
| 042  | Escape unescaped address and BIS fields in email templates | P1 | S | LOW | — | security | DONE |
| 043  | Fix spoofable client IP key in rate limiter | P1 | S | LOW | — | security | DONE |
| 044  | Add authentication to /api/warmup endpoint | P1 | S | LOW | — | security | DONE |
| 045  | Admin security microhardening — social URL validation, logout CSRF, unified 401 | P1 | S | LOW | — | security | DONE |
| 046  | Persist measurementUnit lookups in BlobCache instead of in-memory Map | P2 | S | LOW | — | perf | DONE |
| 047  | Consolidate duplicate createSlug implementations | P2 | S | MED | — | tech-debt | DONE |
| 048  | Delete deprecated cacheUtils.ts re-export shim | P2 | S | LOW | — | tech-debt | DONE |
| 049  | Replace console.log/info with logger in src/lib/ | P2 | S | LOW | — | dx | DONE |
| 055  | Move typescript to devDependencies + collapse dual-version install | P2 | S | LOW | — | dependencies | DONE |
| 050  | Decompose client.ts and break circular import with productUtils.ts | P2 | M | MED | — | tech-debt | DONE (partial — see notes) |
| 051  | Eliminate duplicate catalog pagination loop in categories.ts | P2 | M | MED | 050 (soft) | tech-debt | DONE |
| 052  | Replace window global bus with CustomEvents | P2 | M | LOW | — | tech-debt | DONE |
| 053  | Extract QuickViewController out of QuickView.astro | P3 | M | LOW | — | tech-debt | DONE |
| 054  | Narrow as-any casts in Square layer + Astro transition types | P3 | M | LOW | 050 (soft) | tech-debt | DONE |
| 056  | Upgrade TypeScript from 6.0.3 to 7.x | P3 | M | MED | 055 | migration | BLOCKED (`@astrojs/check@0.9.9`, the latest release, declares `typescript: "^5.0.0 \|\| ^6.0.0"` — no version supports TS 7 yet; `@typescript-eslint/parser` also caps at `<6.1.0`. Revisit when @astrojs/check ships TS 7 support.) |
| 057  | Fix .env.example inaccuracies and document dev script workaround | P3 | S | LOW | — | dx | DONE |
| 058  | Remove dead test-blob-cache.ts script and fix stale README content | P3 | S | LOW | — | dx | DONE |
| 059  | Fire-and-forget inventory cache writes (remove await on Blob writes) | P2 | S | LOW | — | performance | DONE |
| 060  | Switch BlobCache product reads to eventual consistency | P2 | S | LOW | — | performance | DONE |
| 061  | Eliminate N Square API calls in category nav visibility check | P2 | M | MED | — | performance | DONE |
| 062  | Reduce shop page image preloads from 8 to 1 (LCP only) | P3 | S | LOW | — | performance | DONE |
| 063  | Eliminate duplicate image fetch in fetchProduct | P3 | S | LOW | — | performance | DONE |
| 064  | Split shared ApiRetryClient circuit breaker into catalog + checkout instances | P2 | M | MED | — | correctness | DONE |
| 065  | Move rate limiter check before body parse in calculate-cart | P2 | S | LOW | — | correctness | DONE |
| 066  | Escape contact.email in admin notification email templates | P2 | S | LOW | — | correctness | DONE |
| 067  | Fix integer/fractional hour mismatch in pickup time comparison | P2 | S | LOW | — | correctness | DONE |
| 068  | Fix after-hours nextPickupTime can schedule past store close | P2 | S | LOW | 067 (soft) | correctness | DONE |
| 069  | Add unit tests for pure utility libs (filterUtils, slugUtils, shopHours, productUtils, deduplication, errorUtils) | P2 | M | LOW | — | test-coverage | DONE |
| 070  | Add unit tests for admin and public API route handlers | P2 | M | LOW | — | test-coverage | DONE |
| 071  | Add unit tests for inventoryCore fallback path and rateLimit | P2 | M | LOW | — | test-coverage | DONE |
| 072  | Enable The Shop page (admin toggle — product decision required) | P2 | S | LOW | — | direction | DONE (toggle enabled; Tyler completing article content and category images) |
| 073  | Customer-facing order status lookup page | P3 | M | LOW | — | direction | REJECTED (business scale doesn't warrant it — customers can call the shop) |
| 006  | Always set Secure on the admin session cookie | P3 | S | LOW | — | security | DONE |
| 008  | Resolve "square-legacy" naming confusion, remove dead v39 script | P3 | S | LOW | — | dependencies | DONE |
| 009  | Characterize QuickView's variation logic before reuse (spike) | P3 | M | LOW | — | tech-debt | DONE |
| 012  | Correct README type-safety claim, catalog production `any` casts | P3 | M | LOW | — | dx | DONE |
| 014  | Spike: admin bulk order ops / CSV export | P3 | S | LOW | — | direction | DONE |
| 015  | Spike: unify visibility toggles and featureRegistry? | P3 | S | LOW | 003 (soft) | direction | DONE |
| 074  | Fix stale sale prices overwriting cart after navigation (saleInfoGeneration guard) | P1 | M | MED | — | bug | DONE |
| 075  | Separate email-send failure from blob-cleanup failure in webhook handler | P1 | M | LOW | — | bug | DONE |
| 076  | Fix addItem double-click race (no in-flight guard) | P1 | S | LOW | — | bug | DONE |
| 077  | Fix cart undo unawaited + pickup pre-open window missing guard | P1 | S | LOW | — | bug | DONE |
| 078  | Fix admin login open redirect | P1 | S | LOW | — | security | DONE |
| 079  | Fix CSP unsafe-inline via nonce-based approach | P2 | L | MED | — | security | DONE |
| 080  | Security hardening bundle (productUrl scheme, gallery XSS, cookie encoding) | P1 | S | LOW | — | security | DONE |
| 081  | Wrap fetchProducts in BlobCache + fix O(n²) allObjects lookup + dead cache key | P2 | S | LOW | — | performance | DONE |
| 082  | Parallelize inventory + pricing checks in create-checkout | P2 | S | LOW | — | performance | DONE |
| 083  | Parallelize sequential blob reads on The Shop page | P2 | S | LOW | — | performance | DONE |
| 084  | Cache getPage fallback path in wordpress/api.ts | P3 | S | LOW | — | performance | BLOCKED (plan's naive `wordpressCache.set(cacheKey, page)` would corrupt data: `fetchWithCache` stores raw API objects; fallback returns already-processed `WordPressPage`; calling `processPage()` on it again produces `id=0`, nested `title`. Fix: use a separate `processed_page_${slug}` key checked before `fetchWithCache`, or cache the raw page object from `getPages()`. Needs plan revision.) |
| 085  | Add unit tests for src/lib/square/client.ts | P2 | M | LOW | — | test-coverage | DONE |
| 086  | Fill email sender + templates test coverage gaps | P2 | M | LOW | — | test-coverage | DONE (all in-scope email files already ≥ 80% branch: sender.ts 95%, templates.ts 91%, failedEmails.ts 90% — no new tests needed) |
| 087  | Add unit tests for untested admin API routes | P2 | M | LOW | — | test-coverage | DONE |
| 088  | Add unit tests for categories.ts and categoryUtils.ts | P2 | M | LOW | — | test-coverage | DONE (categories.test.ts and categoryNav.test.ts already exist; categoryUtils.ts was renamed to categoryNav.ts in a prior refactor — all coverage confirmed passing) |
| 089  | Add E2E test for complete checkout flow | P2 | M | LOW | — | test-coverage | DONE |
| 090  | Fill small test gaps — batchInventory + quickViewController | P3 | S | LOW | — | test-coverage | TODO |
| 091  | Delete unauthenticated debug catalog endpoint | P1 | S | LOW | — | architecture | DONE |
| 092  | Replace suppressed errors and stray console calls | P2 | M | LOW | — | architecture | TODO |
| 093  | Remove duplicate createSlug in Sidebar.astro | P3 | S | LOW | — | architecture | DONE |
| 094  | Clarify naming in Square category files | P3 | S | LOW | — | architecture | TODO |
| 095  | Decompose client.ts into focused catalog/mapping modules | P3 | L | MED | 081, 085 | architecture | DONE |
| 096  | Decompose create-checkout.ts into lib/checkout/ | P3 | L | MED | 077, 082 | architecture | TODO |
| 097  | Replace astro-seo-metadata with inline SEO tags | P3 | M | LOW | — | dependencies | TODO |
| 098  | Unpin Astro + refresh lockfile | P2 | M | MED | — | dependencies | DONE |
| 099  | Add .env.example and .editorconfig | P2 | S | LOW | — | dx | DONE |
| 100  | Speed up pre-commit hook (scope to staged files) | P2 | S | LOW | — | dx | DONE |
| 101  | Add ESLint and Prettier | P2 | M | LOW | 100 (soft) | dx | TODO |
| 102  | Cross-link sale items from The Shop page | P2 | M | LOW | product decision | direction | REJECTED (The Shop page is an About Us page, not a selling surface — no products displayed there) |
| 103  | Document shop-news convention in admin UI | P3 | S | LOW | — | direction | DONE |
| 104  | Events listing page | P3 | L | LOW | product decision | direction | REJECTED (events already filterable on News page via WordPress tag — no standalone page needed) |
| 105  | Add link field to announcement banner | P2 | S | LOW | — | direction | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 015 has a *soft* dependency on 003: it reads `src/lib/pageVisibility.ts`, which 003 creates. 015 can still run first — its plan file explains how to adjust if 003 hasn't landed yet.
- 002 and 014 both touch `src/pages/admin/orders/*.astro` — no hard ordering requirement, but if both are in flight in the same session, land 002 first so 014's investigation reads the post-singleton code.
- 009 is a prerequisite *investigation* for a future (not-yet-written) QuickView/PDP consolidation plan — it does not by itself reduce the duplication, only characterizes it safely.
- 016 supersedes 013 — same goal (populate sitemap customPages), different approach (alias-free fetchers using the `square-legacy` SDK and a plain WordPress `fetch()` directly, instead of importing `src/lib/square/client.ts`/`categories.ts` into `astro.config.mjs`). Execute 016, not 013.
- 017 and 018 both touch root config (`vitest.config.ts` and `package.json`/`pnpm-workspace.yaml` respectively) but not the same files — no ordering requirement between them.
- 021 and 022 both touch `src/pages/api/calculate-cart.ts`, in different non-adjacent regions (021: the `orders.calculate` call; 022: the `lineItems`/subtotal pricing above it). No hard dependency, but if both are executed as separate worktrees, expect a manual merge step when landing the second one — the diffs don't overlap in content, only in file.
- 024 touches `src/lib/square/client.ts`/`inventoryCore.ts`/`apiUtils.ts`, disjoint from 021's files (`create-checkout.ts`/`calculate-cart.ts`) — no ordering requirement, both reuse the same already-existing `apiRetryClient` pattern from `categoryUtils.ts` independently.
- 024 depends on both 025 and 026 (both now DONE, branches `advisor/025-fix-filterCaching-test-isolation` and `advisor/026-fix-filterPerformance-test-isolation`): Plan 024's Steps 1-2 are committed and correct (in worktree `agent-a42d2506dc84eafcf`, branch `advisor/024-consolidate-square-resilience-layer`) but not yet merged with 025/026's fixes. To resume: merge/rebase 025 and 026 into the 024 branch, then continue its Steps 3-4 (update `inventory.test.ts`, delete the dead `apiUtils.ts` code) — Step 3's verification should no longer hit either test-timeout.
- All other plans are independent and can run in any order or in parallel worktrees.
- Plans 027-033 are all independent of each other and of all prior plans — execute in any order or in parallel worktrees.
- 033 (remove RUM monitoring) has no dependency on 030 (categories.ts cleanup) but both touch unrelated files — safe to run in parallel.
- DEP-02 (Square v45 upgrade) was identified during the 2026-07-22 audit but explicitly deferred to a separate conversation — major breaking changes require dedicated investigation. Do not attempt as part of this batch.
- Plans 034-037 are all independent of each other — execute in any order or in parallel worktrees.
- 039 has a *soft* dependency on 035: if 035 has landed, `escHtml` will already be exported from `templates.ts` and Plan 039's Step 1 simplifies. If 035 hasn't landed yet, 039 must export the helpers itself.
- 040 has a *soft* dependency on 038: if 038 has landed, the webhook handler's email-error path will differ from Plan 040's excerpt — read the live file before writing tests and adjust accordingly.
- 041 (quick wins) is independent of all other plans and safe to run at any time.
- Plans 042–045 (security) are all independent of each other — run in parallel or any order.
- 047 (createSlug consolidation) requires checking live Square category names for underscores before executing — see STOP conditions.
- 051 (duplicate pagination) has a soft dependency on 050: if 050 has landed, `client.ts` may have changed; read the live file before proceeding.
- 054 (as-any narrowing) has a soft dependency on 050: if 050 moved functions to `catalogUtils.ts`, check that file for remaining casts.
- 055 must land before 056 (TypeScript 7 upgrade needs typescript in devDependencies first).
- 057 and 058 are fully independent of all other plans.
- 059 (fire-and-forget cache writes) is independent; run before 060 for a combined perf improvement.
- 061 (category nav N+1) has a soft dependency on fetchProducts being exported from client.ts — check before importing.
- 064 (split circuit breaker) touches all callers of apiRetryClient — reconcile with any in-flight plan that also imports apiRetry.ts (e.g. 065).
- 067 (fractional hours) should land before 068 (after-hours scheduling) — 068 depends on the fixed fractional comparison.
- 069, 070, 071 (test suites) are independent of each other and of source changes — safe to run in parallel worktrees.
- 072 (enable Shop page) is a product decision first; executor must confirm before toggling.
- 073 (order status lookup) is a product decision first; do not implement without business owner confirmation.
- Plans 074-090 (test coverage) are all independent of each other and of source plans — safe to run in parallel worktrees.
- 077 (cart undo fix) and 082 (parallel inventory+pricing) both touch `create-checkout.ts` — land 077 before 082 or expect a manual merge.
- 078 (open redirect) touches both `login.astro` and `admin-auth.ts` — fully independent of other plans.
- 081 (fetchProducts cache) must land before 095 (decompose client.ts) — 081 changes the caching layer 095 refactors around.
- 085 (client.ts unit tests) must land before 095 (decompose client.ts) — tests are the safety net for the refactor.
- 091 (delete list-catalog.ts) — confirm no callers before deleting; see STOP conditions in plan file.
- 095 (decompose client.ts) depends on plans 081 AND 085 being DONE — do not start until both are landed.
- 096 (decompose create-checkout.ts) depends on plans 077 AND 082 being DONE — do not start until both are landed.
- 100 (pre-commit speed) should land before 101 (ESLint) so lint can be wired into the already-scoped hook.
- 102 and 104 (direction: sale cross-link, events page) require product decisions before implementation — see STOP conditions.
- Plans 076-084 (bugs + perf) are highest-priority new work; execute before architecture plans 091-096.

## Findings considered and rejected

Two findings from the initial subagent audit were rejected after the advisor
personally re-read the cited code — recorded here so they aren't re-audited
next run:

- **"Committed credentials in `.env`"**: rejected — false. `.env` is listed in `.gitignore` (`# environment variables` block) and is untracked (`git ls-files` confirms only `.env.example` is tracked). The subagent that reported this read an on-disk file and incorrectly concluded it was committed to git.
- **"Webhook deletes pending-order blob without awaiting (idempotency bug)"**: rejected — false. `src/pages/api/webhooks/square.ts:221` already has `await deletePendingOrder(orderId)`. The cited code does not match the live file.
- **"RequestDeduplicator allows concurrent requests on rejection"**: rejected — not a bug. `.finally()`-based cleanup in `src/lib/square/requestDeduplication.ts` deleting the in-flight key after a promise settles is the intended, correct behavior of that pattern, not a race condition.
- **"Inventory check defaults to 999 in stock on missing data (oversell risk)"**: rejected — false. `src/pages/api/create-checkout.ts:213` defaults missing inventory data to `0` (`inventoryLevels[item.variationId] || 0`), the fail-*closed* direction, not `999`. No such literal exists in the file.
- **"Shipping state-machine off-by-one: marking an already-COMPLETED order silently no-ops"**: rejected — false. `src/pages/api/admin/mark-shipped.ts:66-69` filters to only fulfillments where `state !== "COMPLETED" && state !== "CANCELED"` before computing `currentState`, so `currentState` can never already be `"COMPLETED"` when the state-walk runs; the described scenario cannot occur.
- **"No admin authentication; all /admin/* routes public"** (direction finding): rejected — false, and contradicted the audit's own DOCS-01 finding from a different subagent. `src/middleware.ts` already verifies an HMAC session token on every `/admin/*` route except `/admin/login`. This is exactly the scenario Plan 010 fixes in the docs (which still described this as outstanding work).

Three findings from the test-coverage and tech-debt subagents (the bulk of
`TEST-COVERAGE-2` through `-12`, and several smaller tech-debt items) were
folded into Plan 007 rather than getting individual plans — Plan 007
establishes the pattern on the two highest-value routes; expanding it to the
other ~31 untested API routes and ~12 untested lib modules is real future
work but was judged too large to plan file-by-file without first proving the
pattern works (see Plan 007's "Maintenance notes").

A post-fix re-scan (2026-06-24, against commit `a188e11`) also rejected one
finding from the fresh round of subagents:

- **"`checkItemInventory`'s `locationIds: import.meta.env.PUBLIC_SQUARE_LOCATION_ID` is wrong — should be an array like `batchGetCounts` uses"**: rejected — false. Checked the Square SDK's own type definition (`node_modules/square-legacy/api/resources/inventory/client/requests/GetInventoryRequest.d.ts`): `inventory.get()`'s `locationIds` field is typed `string | null` (a comma-separated list), not an array — that's a different endpoint shape from the batch endpoint's array parameter. The code is correct as written.

That re-scan also produced 5 verified new findings (coverage gate currently
red, 5 high-severity dependency advisories, ineffective `pnpm.overrides`,
and 461 lines of confirmed-dead code) — see Plans 017-019.

## Square deep-dive findings (2026-07-01, commit `56226fb`) — all four planned (021-024)

A focused pass over the cart/checkout/Square integration surfaced the
findings below; the maintainer selected all four for planning.

Correction made during plan-writing: the initial summary presented to the
maintainer described `src/lib/square/apiUtils.ts`'s `CircuitBreaker` as
equivalent to `src/lib/square/apiRetry.ts`'s `ApiRetryClient`. Reading
`apiUtils.ts`'s `execute()` closely shows it calls the wrapped function
**once** — on failure it only increments a counter toward eventually
failing fast, it never retries the call itself. `apiRetryClient` actually
retries with backoff/jitter/timeout and has its own internal circuit
breaker — it's the strictly more capable of the two. This changed which
utility Plans 021 and 024 standardize on (`apiRetryClient`, not
`defaultCircuitBreaker`) and reframed 024 from "delete dead code" to
"migrate the checkout-critical call sites that currently only get
fail-fast protection onto the utility that actually retries, then remove
the now-fully-unused weaker one."

- **Checkout-critical Square calls have no retry protection at all**:
  `squareClient.checkout.paymentLinks.create()` in
  `src/pages/api/create-checkout.ts:390` and `squareClient.orders.calculate()`
  in `src/pages/api/calculate-cart.ts:76` call the SDK directly with no
  wrapper of any kind. A transient Square 5xx/network blip at the exact
  moment a customer pays fails hard with a generic 500, even though
  `apiRetryClient` (real retry+backoff, already proven in
  `categoryUtils.ts`/`Nav.astro`) is one import away. → Plan 021.
- **The checkout-critical inventory/catalog path only has fail-fast
  protection, not retry**: `src/lib/square/client.ts` (`fetchProducts`,
  `fetchProduct`) and `src/lib/square/inventoryCore.ts:58` wrap their Square
  calls in `defaultCircuitBreaker.execute()`, which — per the correction
  above — never retries a failed call, only opens a circuit after repeated
  failures. Migrating these three call sites onto `apiRetryClient` gives
  them actual retry behavior; the now-unused `CircuitBreaker` class in
  `apiUtils.ts` (no remaining callers after migration) can then be removed,
  along with `apiRetry.ts`'s status as a barely-used utility despite
  carrying an enforced per-file coverage threshold in `vitest.config.ts`.
  → Plan 024.
- **`calculate-cart.ts` trusts client-supplied sale prices for the pre-checkout
  total preview**: `src/pages/api/calculate-cart.ts:32` uses
  `item.saleInfo?.salePrice` (attacker-/staleness-controlled via
  localStorage) when building the line items sent to Square's
  `orders.calculate`, unlike `create-checkout.ts:263`, which re-derives
  authoritative pricing via `getAuthoritativePricing()` before charging. No
  money is at risk (checkout re-derives correctly), but the *displayed*
  preview total on the cart page can diverge from what the customer is
  actually charged, which reads as a bug to the customer. Fix: call
  `getAuthoritativePricing()` in `calculate-cart.ts` the same way
  `create-checkout.ts` does. → Plan 022.
- **Free-shipping threshold and flat rate are hand-duplicated in three
  places**: authoritative values live in `src/lib/config/shipping.ts:35-49`
  (`rate: 5.99`, `freeThreshold: 75.0`); `src/components/FulfillmentSelector.astro:11-12`
  redeclares them server-side (`FREE_SHIPPING_THRESHOLD = 7500` cents,
  `FLAT_RATE_SHIPPING = 599` cents) for the tab label; the same file's client
  `<script>` (lines 462-463) redeclares them a third time in dollars for the
  progress-bar widget. A future rate change in `shipping.ts` silently
  desyncs the two display copies. → Plan 023.

Not planned (informational only, low confidence/low severity):
- `create-checkout.ts:601` (cart.astro) generates a fresh `checkoutKey`
  (idempotency key) on every click of "Continue to Payment" rather than
  reusing one across retries of the *same* attempt — if a request succeeds on
  Square's side but the response is lost before the client sees it, a
  follow-up click could create a second payment link/order. Low likelihood,
  and not a double-charge risk (the customer must complete a second payment
  to actually pay twice) — flagged for awareness, not worth a dedicated plan.
- `e2e/cart-flow.spec.ts`'s "should navigate to cart page and display items"
  test asserts button text `"Proceed to Checkout"`, but the live button says
  `"Continue to Payment"` (`src/pages/cart.astro` checkout button). Pre-existing
  test/code drift, unrelated to Plan 020 — worth a one-line fix whenever
  someone is next in that file.

## Full-repo audit findings (2026-07-22, commit `89d7c1d`)

Maintainer selected P1 quick wins + performance cluster for planning (027-033).
Square v45 upgrade (DEP-02) deferred to a separate conversation — prior failed
migration attempts; major breaking changes need dedicated investigation.

Findings considered and not planned (all LOW confidence or LOW severity):

## Fresh deep audit findings (2026-07-22, commit `9b5556b`) — Plans 042-073

Security, architecture, dependencies, and DX categories audited (Plans 042-058).
Performance, correctness, test coverage, and direction subagents were re-run
in the same session after context compaction; those findings yielded Plans 059-073.

**Not planned** from this audit batch:
- **DEPS-04 (Square v44 internal bundle of v39)**: informational; no fix possible without v45 migration (already explicitly deferred — see earlier note above about DEP-02).
- **ARCH-03 (window.openQuickView global)**: deferred to a follow-up of plan 052 once the four primary globals are migrated.
- **DX-08 (README "Last updated" date)**: folded into plan 058.

- **WPColumns.astro `set:html` with raw column content**: `src/components/WPColumns.astro:25` passes `column.content` (extracted by regex from WordPress HTML) directly to `set:html`. WordPress is a trusted CMS (not public user input), so XSS risk is LOW — not worth hardening unless CMS access controls are weakened. Same gap exists in `WPEmbed.astro:129`.
- **Order email PII in logs**: `src/lib/email/` logs the recipient address at INFO level. LOW severity since this is admin-only infrastructure; acceptable until structured logging is added.

**Not planned** from the performance/correctness/test/direction batch (Plans 059-073):

- **PERF-06 (`extractFilterOptions` O(N×M) not cached)**: the function is cheap enough in practice — product catalogs are small (< 500 items); no measurable regression observed. Deferred.
- **PERF-07 (weak fingerprint in `filterProductsWithCache`)**: PLAUSIBLE confidence only; cache key uses product IDs which should be stable unless a variant's ID changes. Deferred.
- **PERF-08 (`batchGetImageUrls` double-checks for known-uncached IDs)**: S effort but requires reading `imageUtils.ts` carefully to avoid introducing cache-skip bugs. Deferred.
- **DIRECTION-03 (Events as first-class page)**: `WPEventBlock.astro` and `event-reference.astro` exist but no `/events` route. Product decision needed first — not worth a plan without business intent confirmed.
- **DIRECTION-04 (Promo/discount code at checkout)**: Square Discounts API unused. Good candidate for a future `/improve next` session when the business is ready.
- **DIRECTION-05 (Square Loyalty integration)**: L effort, requires Square Loyalty to be enabled on the account. Not planned without business confirmation.
- **TEST-02 (`inventoryCore.ts` batch-failure fallback) and TEST-05 (`rateLimit.ts`)**: folded into Plan 071.
- **TEST-01, TEST-04, TEST-09, TEST-10, TEST-12** (filterUtils, slugUtils, productUtils, errorUtils, requestDeduplication): folded into Plan 069.
- **TEST-06, TEST-07, TEST-08, TEST-11** (mark-shipped, mark-pickedup, shop-status, back-in-stock, retry-failed-emails): folded into Plan 070.

## Deep audit findings (2026-08-01, commit `915a062`) — Plans 074-105

All 37 non-direction findings and 4 direction findings from the fresh deep audit were turned into plans (076-105; 074-075 from prior session). No findings were rejected or deferred from this batch — all were in scope and had clear implementation paths.

The only STOP-condition items are product decisions embedded inside individual plans:
- Plan 102 (sale cross-link): what Square field marks an item on sale?
- Plan 104 (events listing): where does events data live?

## Legal/disclaimer review (2026-07-01) — advisory, no code changes proposed

- Existing coverage is solid: `src/components/CartFooter.astro` renders
  WordPress-backed policy links (privacy-policy, return-policy,
  shipping-policy, terms-and-conditions via `getLegalPages()` in
  `src/lib/wordpress/api.ts:406`) as an in-page modal on every cart/checkout
  page, with sane fallback content if WordPress is unreachable
  (`src/pages/legal/[slug].astro:48-102`). A physical-gift-card shipping
  disclaimer already exists and is conditionally shown
  (`src/components/FulfillmentSelector.astro:166-186`).
- No sitewide analytics/tracking scripts were found (`src/components/BaseHead.astro`
  and a repo-wide grep for `gtag`/`googletagmanager` turned up nothing
  cart/checkout-relevant), so a cookie-consent banner is likely not required
  today — revisit if analytics/ad pixels are added later.
- Not present: a CCPA-style "Do Not Sell or Share My Personal Info" /
  "Your Privacy Choices" link, which the Shopify reference screenshot shows
  in its footer. Whether this is legally required depends on revenue/data
  volume thresholds this review can't determine from the code — flagging so
  the maintainer can check with counsel; if needed, it's a cheap addition
  (one more footer link/modal, same pattern as the existing four).
- Afterpay/Clearpay (BNPL) disclosures are handled entirely on Square's own
  hosted checkout page (`linkResponse.paymentLink.url` in
  `create-checkout.ts:390`) — outside this codebase's control or
  responsibility.
- Sales tax is computed by Square (`pricingOptions.autoApplyTaxes: true`) and
  already shown as a line item pre-checkout — no gap found.

## Final audit findings (2026-07-22, commit `0da82aa`) — selected 7 for planning (034-041)

Plans 034-040 cover the selected findings. Plan 041 bundles the quick-win
package/dead-code cleanup.

Findings considered and **not planned** (low severity, by-design, or deferred):

- **`clearCategoryProductCache()` is a silent no-op** (`categoryUtils.ts:258-273`): the function casts to `any` and reads `.cache` which doesn't exist on `BlobCache`. No external callers exist. Rolled into Plan 041 (delete or fix).
- **`pruneInventoryCache()` wraps a documented no-op** (`inventory.ts:147-149`): `BlobCache.prune()` always returns 0 per its own comment. No active production consequence; rolled into Plan 041.
- **`_fetchAllProductsFromCategory` dead code** (`categories.ts:377-394`): unexported, no callers. Rolled into Plan 041.
- **`cleanupClientState()` exported but never called** (`client.ts:558-565`): rolled into Plan 041.
- **`web-vitals` in `dependencies`, never imported**: rolled into Plan 041.
- **`@astrojs/check` in `dependencies` instead of `devDependencies`**: rolled into Plan 041.
- **`.env.example` missing Square circuit-breaker vars + `PUBLIC_WORDPRESS_API_URL`**: rolled into Plan 041.
- **Empty `src/lib/monitoring/` directory**: rolled into Plan 041.
- **PERF-02: Dual full-catalog cache** (`client.ts:192` and `categories.ts:145`): L effort for warmup-window benefit only; deferred.
- **PERF-03: shop/all.astro preloads 8 images**: S effort; deferred — not selected.
- **PERF-04: PDP related-products waterfall**: S effort; deferred — not selected.
- **PERF-05: imageUtils per-image API calls**: M effort; deferred — not selected.
- **COV-01 (pdpController.test.ts mocks)**, **COV-02 (pdpUI.ts zero tests)**, **COV-05-08**: Real gaps; deferred to a future test-coverage-focused session rather than adding 8 more plans here.
- **ARCH-04-09** (slug duplication, `as any` cluster, measurementUnitCache, etc.): maintenance items; deferred.
- **SEC-03: Admin logout CSRF** (`admin-logout.ts:4`): LOW risk (forced-logout only, no privilege escalation); not worth a plan.
- **SEC-04: Error detail in admin pickup reminder** (`send-pickup-reminder.ts:70`): LOW risk (admin-only endpoint, trusted caller); not planned.
- **DIR-01: Product search**, **DIR-03: Admin settings extension**, **DIR-04: Customer accounts**: direction items, not bugs. Good candidates for the next `/improve next` session.
- **WPColumns.astro `set:html`**: WordPress is a trusted CMS (not public user input); by-design, not a finding (same rejection as the 2026-07-22 batch).
- **README stale badges + wrong file paths + pnpm test vs test:run discrepancy**: fixed directly in README.md in this session; no plan needed.
- **`docs/TYPE_SAFETY_NOTES.md` stale commit reference**: LOW confidence, LOW impact; not planned.
