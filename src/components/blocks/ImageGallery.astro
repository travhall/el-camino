---
import type { Block } from '@/lib/types/content';

interface Props {
  block: Block;
}

const { block } = Astro.props;
---
<div class={`grid grid-cols-${block.columns || '2'} gap-4`}>
  {block.images?.map(image => (
    <img src={image.src} alt={image.alt} class="w-full h-auto" />
  ))}
</div>